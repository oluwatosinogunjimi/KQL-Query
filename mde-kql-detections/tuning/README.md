# Tuning Notes

This folder tracks tuning decisions and exclusions for detection rules.
Each rule can have a corresponding tuning log file.
Use these logs to document false positives, suppression logic, and threshold updates.
The objective is to reduce alert noise without losing meaningful detection coverage.
Review and update tuning entries as environments and threats evolve.
